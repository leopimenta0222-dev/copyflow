# ---- Cliente (Vite) ----
# Supabase (opcional — sem isto, login e histórico rodam em modo demo local)
VITE_SUPABASE_URL=https://SEU-PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=sua-anon-key-aqui

# ---- Servidor (função /api/generate — NUNCA exponha no front) ----
# Chave da Groq (gratuita — crie em console.groq.com). Sem ela, a geração usa
# um fallback de exemplo (modo demo).
GROQ_API_KEY=gsk_xxx
